// TODO: Finish admin first
export interface Event {
    id: number,
    event_desc: string,
    event_name: string,
    event_img: string,
    event_max: number,
    event_dstart: number,
    event_dend: number,
};